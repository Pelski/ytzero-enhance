{
  "name": "ytzero-enhance",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "bun scripts/build.ts",
    "test": "bun test",
    "check": "bunx tsc --noEmit && bun test && bun scripts/build.ts --check",
    "package": "bun scripts/build.ts --package",
    "safari:project": "bun run build && xcrun safari-web-extension-converter dist/safari --project-location safari --app-name 'YT Zero Enhance' --bundle-identifier pl.pelski.ytzero-enhance --swift --copy-resources --no-open --no-prompt --force"
  },
  "devDependencies": {
    "@types/bun": "^1.3.6",
    "typescript": "^5.9.3"
  }
}
